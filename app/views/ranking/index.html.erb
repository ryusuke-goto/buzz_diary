<% if @max_liked_diaries.present? %>
  <div id="diaries">
    <div class="text-2xl items-center justify-center">
      いいねを一番もらった日記は...
    </div>
    <%= render partial: "diaries/diary", collection: @max_liked_diaries %>
  </div>
<% else %>
  <div class="flex items-center justify-center text-2xl">
    <%= t("diaries.no_result") %>
  </div>
<% end %>
<% if @max_liked_diaries.present? %>
  <div id="diaries">
    <div class="text-2xl items-center justify-center">
      いいねを一番もらった日記は...
    </div>
    <%= render partial: "diaries/diary", collection: @max_likes_record_users %>
  </div>
<% else %>
  <div class="flex items-center justify-center text-2xl">
    <%= t("diaries.no_result") %>
  </div>
<% end %>