<div class="container">
  <div class="">
    <div class="">
      <div class="mx-10">
        <% if @diaries.present? %>
          <%= render partial: "diary", collection: @diaries %>
        <% else %>
          <div class="flex items-center justify-center text-2xl">
            <%= t("diaries.no_result") %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>