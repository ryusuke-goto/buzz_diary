<div class="container">
  <!-- 日記一覧 -->
  <div class="">
    <div class="">
      <div class="flex items-center justify-center mx-10">
        <% if @diaries.present? %>
          <%= render partial: "diary", collection: @diaries %>
        <% else %>
          <div class="text-2xl">
            <%= t("diaries.no_result") %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>