<table>
  <thead>
    <tr>
      <th>ミッションID</th>
      <th>タイトル</th>
      <th>ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% daily_missions.each do |mission| %>
      <tr>
        <td><%= mission.id %></td>
        <td><%= mission.title %></td>
        <td>
          <% if daily_status[mission.id]&.status %>
            達成済
          <% else %>
            未達成
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>